for file in web/index.html web/dashboard.html web/ledger.html web/api.html; do
  # It's easier to just use sed to replace the nav block, or since I don't want to break things,
  # I'll let sed do it if I can safely match. Actually I can just write a python script.
done
